A small project in Unity, which recreates the fibonacci sequence in C# (Unity)
It uses debug.log to show the output of the sequence, the output numbers can be adjusted in the code (infinite would obviously just crash)
